---
import TerminalShell from "../layouts/TerminalShell.astro";
import TuiPanel from "../components/chrome/TuiPanel.astro";
import { publicCopy } from "../content/site";

const locale = "en_US";
const copy = publicCopy[locale];
---

<TerminalShell locale={locale} title={`${copy.notFound.heading} | ${copy.title}`} description={copy.description} nav={copy.nav}>
  <TuiPanel title={copy.notFound.heading}>
    <h1>{copy.notFound.heading}</h1>
    <p>{copy.notFound.intro}</p>
    <p><a href="/en_US/">{copy.shell.returnHome}</a></p>
  </TuiPanel>
</TerminalShell>
