---
import TerminalShell from "../layouts/TerminalShell.astro";
import TuiPanel from "../components/chrome/TuiPanel.astro";
import { publicCopy } from "../content/site";

const locale = "en_US";
const copy = publicCopy[locale];
---

<TerminalShell locale={locale} title={copy.title} description={copy.description} nav={copy.nav}>
  <TuiPanel id="locale-gateway" title={copy.gateway.heading} as="nav">
    <h1>{copy.gateway.heading}</h1>
    <p>{copy.gateway.intro}</p>
    <ul class="command-list">
      <li><a href="/en_US/">English</a></li>
      <li><a href="/pt_BR/">Português</a></li>
    </ul>
  </TuiPanel>
</TerminalShell>
